import ModuleAccessLayout from "@/components/ModuleAccessLayout";

export default function CostsLayout({ children }: { children: React.ReactNode }) {
  return <ModuleAccessLayout module="COSTS">{children}</ModuleAccessLayout>;
}
