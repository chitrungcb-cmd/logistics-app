import ModuleAccessLayout from "@/components/ModuleAccessLayout";

export default function DebtsLayout({ children }: { children: React.ReactNode }) {
  return <ModuleAccessLayout module="DEBTS">{children}</ModuleAccessLayout>;
}
