import ModuleAccessLayout from "@/components/ModuleAccessLayout";

export default function OtherExpensesLayout({ children }: { children: React.ReactNode }) {
  return <ModuleAccessLayout module="OTHER_EXPENSES">{children}</ModuleAccessLayout>;
}
