import ModuleAccessLayout from "@/components/ModuleAccessLayout";

export default function ShipmentsLayout({ children }: { children: React.ReactNode }) {
  return <ModuleAccessLayout module="SHIPMENTS">{children}</ModuleAccessLayout>;
}
